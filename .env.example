# Copy this file to `.env` and fill in values locally.
# Never commit real secrets.

# Feature flag: when "true", American Football pages call the real API
# instead of using mock data.
VITE_ENABLE_AMERICAN_FOOTBALL_API=true